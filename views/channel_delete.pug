extends layout

block content

  h1 Delete Channel: #{channel.channel_name}
  
  p Do you really want to delete this Channel?
    
    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=channel._id )

      button.btn.btn-primary(type='submit') Delete